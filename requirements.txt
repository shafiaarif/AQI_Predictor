# Dashboard dependencies (app.py) — Streamlit Cloud root requirements file.
# Hopsworks intentionally NOT included: predict.py's live_features path doesn't
# need it at runtime, and hopsworks' protobuf<5.0.0 requirement conflicts with
# streamlit's protobuf>=5.26.1 requirement. Pipeline deps live in
# requirements-pipeline.txt instead.

streamlit==1.63.0
plotly==7.0.0
shap==0.51.0
pandas==2.3.3
numpy==2.4.6
scikit-learn==1.9.0
catboost==1.2.10
tensorflow==2.21.0
joblib==1.6.0
requests==2.34.2
python-dateutil==2.9.0.post0
pytz==2026.3.post1