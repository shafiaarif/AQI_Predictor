# Feature pipeline + training pipeline dependencies.
# Used explicitly by .github/workflows/feature-pipeline.yml and
# training-pipeline.yml (NOT the Streamlit Cloud dashboard — that uses the
# root requirements.txt, which deliberately excludes hopsworks to avoid the
# protobuf version conflict with streamlit).
#
# NOTE: tensorflow is left UNPINNED on purpose. hopsworks==5.0.6 requires
# protobuf<5.0.0, while the latest tensorflow (2.21.0) requires protobuf>=6.31.1
# — a direct conflict, and both packages must live in the same environment
# here (train_model.py uses hopsworks to fetch data AND tensorflow to train
# the NN in one script). Leaving tensorflow unpinned lets pip's resolver pick
# an older tensorflow release whose protobuf range overlaps with hopsworks'.
# This may differ slightly from the exact local dev version — acceptable for
# the automated retraining job.

hopsworks==5.0.6
pandas==2.3.3
numpy==2.4.6
scikit-learn==1.9.0
catboost==1.2.10
xgboost
tensorflow
joblib==1.6.0
requests==2.34.2
python-dateutil==2.9.0.post0
pytz==2026.3.post1